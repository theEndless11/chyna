{
  "name": "shorts-api",
  "version": "1.0.0",
  "dependencies": {
    "aws-sdk": "^2.1490.0",
    "formidable-serverless": "^1.1.1",
    "fluent-ffmpeg": "^2.1.2",       // added because you use ffmpeg in your upload API
    "ffmpeg-static": "^5.1.0"        // recommended to bundle ffmpeg binary easily
  }
}
